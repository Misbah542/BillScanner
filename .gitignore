# ---- Node / monorepo ----
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/
.env
.env.*
# The only exceptions are the templates: placeholders that name the variables and
# carry no values. A real value belongs to the environment that runs the code, or
# to GitHub Actions secrets — never to the repository.
!.env.example
!.env.test.example
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# ---- Android / Gradle ----
.gradle/
apps/android/local.properties
apps/android/app/build/
apps/android/build/
captures/
*.apk
*.aab
*.keystore
*.jks
!debug.keystore
.cxx/

# ---- Prisma / db ----
apps/api/prisma/*.db
apps/api/prisma/*.db-journal

# ---- Uploads (local storage driver) ----
apps/api/.storage/
apps/api/.storage-test/

# ---- IDE / OS ----
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
